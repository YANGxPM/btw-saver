# UX 改进提案：Claude Code 中 /btw 回答的持久化问题

**作者：** Sheng  
**日期：** 2026-05  
**类型：** UX 改进提案 + 可运行插件实现  
**受众：** Claude Code 用户

---

## 问题定义

### 不可见的状态边界

Claude Code 的 `/btw` 命令会创建一个临时侧边对话——一个可关闭的浮层，在不污染主上下文的情况下回答问题。关闭后，回答永久消失。

这个设计本身是合理且正确的：

- 保持主上下文精简（不积累无关 token）
- 明确传达"这是边注，不是正文"的语义
- 防止探索性问题意外污染主对话上下文

**问题所在：** 用户在回答消失之前，没有收到任何提示。

---

## 用户旅程：问题发生在哪里

```
用户输入 /btw
       ↓
浮层出现——外观与普通 Claude 回答完全一致
       ↓
用户阅读回答
       ↓
用户按下 Escape（或 Space / Enter）
       ↓
回答消失。没有警告。没有恢复路径。
       ↓
用户意识到想保留这条回答——已经太晚了。
```

这直接对应 **Nielsen 可用性启发式第一条：系统状态的可见性（Visibility of System Status）。**

浮层的外观和交互感与普通回答完全相同，没有任何视觉线索区分"临时内容"与"持久内容"。用户无法感知自己正处于一个不保存状态的临时上下文中——直到它消失。

---

## 受影响的用户群体

这个问题对以下群体的影响最为显著：

- **Claude Code 新用户**：尚未理解 `/btw` 的临时性本质
- **深度专注工作中的用户**：在任务进行中用 `/btw` 提了一个问题，得到有价值的回答，但在上下文切换之前忘记保存
- **ADHD 倾向用户**：`/btw` 功能在 Reddit 上被社区称为"送给我们这些 ADHD 脑子的 Claude Code 用户的礼物"。这类用户最容易在保存前关闭浮层

---

## 设计原则

> **默认临时，主动持久。**

目标不是让 `/btw` 变成持久存储——那会破坏它的设计意图。目标是在最关键的时刻（关闭之前），给用户一个**清晰、低摩擦的主动保存选项**。

---

## 核心修复方案（建议 Anthropic 实施）

### 在 /btw 浮层中加入保存按钮

在浮层底部的操作区，与现有的关闭操作并列加入一个"保存"动作。

**现有底部操作区：**
```
[Space / Enter / Esc 关闭]
```

**改进后：**
```
[Space / Enter / Esc 关闭]    [S] 保存到日志
```

**保存行为：**
- 将本次问答追加写入项目根目录下的 `.btw-log/btw-log.md`
- 关闭前短暂显示"已保存 ✓"的确认提示
- 不打断工作流——一键完成保存并关闭

**为什么这是正确的修复方向：**
- 认知成本为零——选项在回答可见时就出现，不需要用户事后回忆
- 非侵入式——默认行为不变，完全由用户主动选择
- 与浮层现有的键盘操作风格保持一致
- 工程实现成本低：一个快捷键绑定 + 一次文件写入

### 次选方案：关闭时的状态栏提示

更柔和的替代方案：当用户按下 Escape 时，在状态栏短暂显示一行提示：

```
/btw 回答已关闭 · 运行 /btw:save <问题> 可重新生成并保存
```

这在不改动浮层布局的前提下，提供了一条恢复路径。

---

## 插件方案的局限性

由于 `/btw` 回答在架构层面是临时的——它们从不进入对话历史——**任何插件都无法在关闭后捕获原始回答。** 本仓库的插件通过重新生成回答来实现近似效果，功能上等价，但内容可能与原始回答略有差异。

| | 理想方案（保存按钮） | 本插件 |
|---|---|---|
| 触发时机 | 关闭前，一键完成 | 关闭后，运行 `/btw:save` |
| 回答还原度 | 精确 | 重新生成（可能略有差异） |
| 操作摩擦 | 接近零 | 低 |
| 需要修改核心代码 | 是 | 否 |

插件是有实际价值的替代方案。保存按钮才是正确的长期解决方向。

---

## 参考资料

- [Claude Code /btw 官方文档](https://code.claude.com/docs/en/interactive-mode)
- [/btw 源码分析——ZhangHanDong](https://gist.github.com/ZhangHanDong/a123f194fc0e68c9d408355bd10746c6)
- [Claude Code 插件系统文档](https://code.claude.com/docs/en/plugins)
- Nielsen Norman Group — 用户界面设计的十大可用性启发式原则（第一条：系统状态的可见性）
- Reddit r/ClaudeAI — 社区对 /btw 功能发布的讨论，2026 年 3 月
